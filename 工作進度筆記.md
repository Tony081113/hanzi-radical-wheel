# 文字拉霸機：工作進度

更新日期：2026-06-21

## 已完成

- 建立偏旁組字輪盤前端：依字形方向選擇偏旁，答對後開啟教育部辭典詞條。
- 題庫爬蟲會從教育部辭典與 IDS 構形資料建立 `questions.json`，並篩除生僻字與過高筆畫字。
- 遊戲頁加入「重爬題目並刷新題庫」：顯示掃描與查核進度條，完成後自動載入新題庫。
- 重爬服務只監聽本機位址，並要求每次啟動時產生的更新密鑰，避免未授權啟動爬蟲。
- 答對一題後，會短暫顯示成功訊息並自動進到下一題。
- 專案已連結至 GitHub 私人 repository：`Tony081113/hanzi-radical-wheel`。

## 啟動方式

```powershell
.\run_game.ps1
```

開啟 `http://localhost:8000` 後，若要重爬題庫，將 PowerShell 顯示的本次更新密鑰貼到頁面欄位。密鑰只在此次服務啟動期間有效。

## 驗證紀錄

- `python -m py_compile app.py scrape_dictionary.py` 通過。
- 未提供或提供錯誤更新密鑰時，`POST /api/refresh-questions` 會回傳 401。
- `node --check game.js` 通過。

## 後續可做

- 抽樣檢查新題庫，持續調整常見字與部件白名單。
- 視需求提供題數、部首掃描範圍與是否接受生僻字的頁面設定。
- 視使用情境決定是否將私人 repository 改為公開。
