# 文字拉霸機：工作筆記

## 專案用途

以偏旁輪盤方式進行組字練習。中央顯示一個部件，使用者在正確的左、右、上或下方選擇偏旁；答對後開啟教育部《重編國語辭典修訂本》的對應詞條。

## 主要檔案

- `index.html`、`style.css`、`game.js`：遊戲前端。
- `scrape_dictionary.py`：題庫建立器。
- `run_crawler.ps1`：互動式 Windows 啟動器。
- `questions.json`：最近一次爬取產生的題庫。
- `ids.txt`：IDS 漢字構形快取，用來判斷左右／上下結構。
- `unihan_strokes.json`：首次以「不接受生僻字」模式執行時，自動下載建立的筆畫快取。

## 題庫建立流程

1. 在 PowerShell 執行 `./run_crawler.ps1`。
2. 選擇題數與要掃描的部首索引數量。
3. 選擇是否接受生僻字。
4. 以 `python -m http.server 8000` 啟動本機伺服器，瀏覽器開啟 `http://localhost:8000`。

## 目前的品質規則

在「不接受生僻字」模式下：

- 答案字必須在教育部《國語辭典簡編本》收錄。
- 答案總筆畫不超過 18。
- 中央部件與作答偏旁必須是常見字或常用部首。

這些規則是為了避免出現冷僻字或不適合一般學習者辨識的構件。若仍有不自然題目，可優先調低 `--max-common-strokes` 或補強 `FAMILIAR_RADICALS` 白名單。

## 注意事項

- 對教育部辭典網站的請求設有 1.2 秒間隔，請勿移除。
- `--max-radicals 0` 代表掃描全部官方部首索引，耗時會明顯增加。
- IDS 用於字形結構判讀；目標字與跳轉詞條由教育部辭典爬取取得。
